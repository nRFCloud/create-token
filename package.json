{
  "name": "@nrfcloud/create-token",
  "version": "0.0.0-development",
  "description": "API endpoint which creates an identity token to be used for authenticating the user against the API",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:coverage-travis": "npm run test:coverage && codeclimate-test-reporter < ./coverage/lcov.info",
    "lint": "standard",
    "precommit": "npm run lint && npm test",
    "commitmsg": "commitlint -e",
    "semantic-release": "semantic-release",
    "travis-deploy-once": "travis-deploy-once"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nRFCloud/create-token.git"
  },
  "keywords": [
    "nRFCloud",
    "IoT",
    "NordicSemiconductor"
  ],
  "author": "Nordic Semiconductor ASA | nordicsemi.no",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/nRFCloud/create-token/issues"
  },
  "homepage": "https://github.com/nRFCloud/create-token#readme",
  "engines": {
    "node": ">=6.10.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^6.0.2",
    "@commitlint/config-angular": "^6.0.2",
    "aws-sdk": "^2.185.0",
    "codeclimate-test-reporter": "^0.5.0",
    "husky": "^0.14.3",
    "jest": "^22.1.4",
    "semantic-release": "^12.4.1",
    "standard": "^10.0.3",
    "travis-deploy-once": "^4.3.3"
  },
  "dependencies": {},
  "files": [
    "index.js",
    "api",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "jest": {
    "coverageReporters": [
      "lcov"
    ]
  }
}
